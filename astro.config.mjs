import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://toffeehpa.github.io',
  // base: '/'  // Не обязательно для репозитория username.github.io, но можно оставить
});
